# 前端开发约定

本文件适用于本前端目录中的所有开发、修复与重构。开始修改前必须显式阅读本文件。

## 分支与交付流程

1. 每项新功能、缺陷修复或独立重构都必须在修改任何文件前新建分支。
2. 分支名按工作类型使用 `feat/*`、`fix/*` 或 `refactor/*`。
3. 一个分支只处理一个主题，不格式化、不修改、不提交无关文件。
4. 完成后先在本地提交，并向用户展示变更摘要、自动检查结果和人工验收说明。
5. 必须等待用户明确确认没有问题后，才可以推送分支并创建 PR。
6. PR 只创建，不自动合并；合并由用户决定。

## Token 使用规则

1. 能使用现有语义 Token 时必须使用 Token。
2. 业务页面和业务组件样式只允许使用 `--ax-*` 语义 Token，不得引用原始色值变量。
3. 业务 Less 禁止新增硬编码颜色、重复阴影或重复尺度。
4. 仅当现有语义 Token 无法表达可复用的组件状态时，才增加组件 Token；不得为单次例外创建 Token。
5. 原始颜色只维护在 `src/styles/primitives.less`，语义映射维护在 `src/styles/tokens.less`。
6. NutUI 主题映射只维护在 `src/styles/theme.less`。

## 组件使用优先级

开发时按以下顺序复用，能使用组件库就使用组件库：

1. 现有业务组件；
2. NutUI 组件；
3. Taro 原生组件组合；
4. 自建组件。

若必须自建组件，应在变更说明中记录 NutUI 不适用的具体原因。不要封装与 NutUI 同义、只做透传的组件。

NutUI 样式必须按组件引入，禁止恢复 `@nutui/nutui-react-taro/dist/style.css` 全量样式。

## 可访问性与交互

1. 主要触控目标最小高度为 `88rpx`。
2. 状态不得仅靠颜色表达；风险、成功、警告等状态必须同时提供文字和图标或其他非颜色线索。
3. 所有异步操作应有加载、禁用和失败反馈，防止重复提交。
4. 文本布局需覆盖长文件名、小屏幕和字体放大场景。
5. 动效必须使用既有时长 Token，并尊重减少动态效果偏好。

## 完成标准

提交前执行 `pnpm verify`，并确认：

- TypeScript、Node 测试、ESLint、设计系统检查与微信小程序生产构建全部通过；
- `app-origin.wxss` 小于 `244 KiB`；
- 按需登录、`authorization` 请求头、受保护导航、登录过期、上传选择/禁用/加载行为无回归；
- 主要页面的正常、按压、禁用、加载、空、成功和风险状态已完成验收说明。
